version: 2
jobs:
  build:
    docker:
      - image: circleci/ruby:3.1.2

    steps:
      - checkout

      - run:
          name: Saying Hello
          command: |
            echo 'Hello World123!'
      
